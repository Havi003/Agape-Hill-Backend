# Spring Boot treats .env as a standard properties file through the extension hint.
# The file is optional so CI can provide the same keys as environment variables.
spring.config.import=optional:file:.env[.properties]

